_format_version: "2.1"
_transform: true

services:
  - name: mock
    url: http://mock:8080/echo
    protocol: http
    routes:
      - name: mock
        paths:
          - /echo

plugins:
  - name: api-catalog-harvester
    enabled: true
    service: mock
    config:
      active: true
